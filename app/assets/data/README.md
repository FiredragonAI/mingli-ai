# 数据文件

## kangxi_strokes.json

康熙字典笔画字典,格式:

```json
{ "王": {"s": 4, "r": "玉", "e": "土"}, "李": {"s": 7, "r": "木", "e": "木"} }
```

- `s` 康熙笔画(必填)
- `r` 部首(用于生肖用字宜忌,选填)
- `e` 字义五行(木火土金水,选填)

未放置时程序使用 `stroke_dictionary.dart` 内置的常用字兜底。
